# 인수인계 — 부산대학교 AI대학 홍보 페이지

**작성 2026-09-08** · 이 문서는 **코드만 봐서는 알 수 없는 것**을 적습니다.
파일이 무엇인지는 `INDEX.md`, 수치가 무엇인지는 `시안_20260822/FACTS_V7.md` 를 보십시오.

> **먼저 읽을 순서**
> 1. 이 문서 §1 · §5 (무엇이고, 어디가 함정인가)
> 2. `시안_20260822/FACTS_V7.md` — **유일한 사실 출처.** 다른 문서의 수치는 전부 낡았습니다
> 3. `시안_20260822/CONTEXT.md` — 「초딩같다」·「진지하다」 같은 판단 용어의 정의
> 4. `시안_20260822/DESIGN_SPEC.md` §1·§2 — 클라이언트 요구 원문과 서버 실측

---

## 1. 무엇인가 / 어디까지 됐나

### 목적

2027년 3월 출범하는 **부산대학교 AI대학**의 홍보 웹페이지. 지금 단계의 산출물은
**「어느 디자인으로 갈지 고르게 하는 시안」** 이지 완제품이 아닙니다.
최종적으로는 기존 `arise-ai.pusan.ac.kr` (React SPA) 안에 정식 라우트로 통합하는 것이 계획입니다(§3).

### 현재 상태 — 시안 2종이 Netlify 에 올라가 있습니다

| 시안 | 주소 | 구성 | 상태 |
|---|---|---|---|
| **AG · 밝은 지면** | https://pnu-ai-college-ag.netlify.app | 4장 (메인 / 정원조정 / AX융합전공 / 교원) | **주력.** 반응이 가장 좋았던 방향 |
| **A · 다크 시네마틱** | https://pnu-ai-college-a.netlify.app | 1장 (내용 전부 한 페이지) | 비교용 |

둘 다 `noindex` + `robots.txt` + `X-Robots-Tag` 로 검색엔진에서 막아 두었습니다.
**승인 전 계획값과 교원 실명이 공개 URL 에 올라가 있기 때문입니다. 이 보호를 풀지 마십시오.**

### 동작하는 것

- AG 4장 전부 · A 1장. 데스크톱 1280 이상 기준
- 스크롤 연동 모션(GSAP ScrollTrigger + CSS 스크롤 주도). **JS 가 죽어도 콘텐츠는 전부 읽힙니다** — 의도된 설계입니다(§5)
- `python check.py` 전수 검증 통과 (56 pages, 5 assets)
- 대비(WCAG AA) 위반 0건 — AG 기준 최저 4.55:1

### 동작하지 않는 것 / 안 한 것

- **모바일 대응 없음.** 데스크톱만 봤습니다
- **Part 1(개요 페이지)은 만들다 중단**했습니다 → §6
- 시안 25종 중 **AG·A 를 뺀 나머지는 전부 옛 수치(`데이터사이언스학부 114명`)** 를 그대로 갖고 있습니다.
  `index.html` 색인과 `sitemap.html` 도 마찬가지입니다. **정정 안 됐습니다** → §6
- 실서버(`arise-ai.pusan.ac.kr`)에는 **아무것도 배포하지 않았습니다.** 읽기 조사만 했습니다

---

## 2. 파일·폴더 지도

### 루트

| 위치 | 무엇 | 왜 여기 |
|---|---|---|
| `HANDOVER.md` | 이 문서 | 처음 읽을 것 |
| `INDEX.md` | 폴더 전체 색인 (2026-08-22 작성) | 파일 하나하나의 정체가 여기 있습니다. **단 폴더 다섯 개를 `_보관/` 아래로 내렸으므로 그 경로 앞에는 `_보관/` 을 붙여 읽으십시오** |
| `시안_20260822/` | **본체.** 시안 소스 전부 + git | 아래 표 참조 |
| `docs/` | **공식 확정 자료** — hwpx·xlsx·pdf 원문 4개 + `지식베이스/`(정리본 9편) + `변환원문/` | 수치가 어긋나면 **여기가 맞습니다** |
| `assets-all/` | 영상·이미지 소재 풀 85개 (218MB, 5세트) | 시안이 쓰는 영상의 원본. `05_레퍼런스_화면녹화/` 만 소재가 아니라 **참고 레퍼런스**입니다 |
| `기획/` | 기획안 JSON 2개 · `디자인_디렉션.md` · `이름변경_기록.tsv` | 참고용. **`디자인_디렉션.md` 은 함정입니다** → §5-6 |
| `_보관/` | 중복본·외부 저장소 사본·2차 자료 | 지우지 않고 내리기만 했습니다. `_보관/README.md` 참조 |

### `시안_20260822/`

| 위치 | 무엇 | 비고 |
|---|---|---|
| **`FACTS_V7.md`** | **확정 수치의 유일한 출처** | 2026-08-23 공식자료 기준. 이것과 다른 숫자는 전부 틀린 것 |
| `CONTEXT.md` | 판단 용어 사전 | 「초딩같다」가 무슨 뜻인지 여기 정의돼 있습니다 |
| `docs/adr/0001-…md` | 디자인 방향을 어떻게 정했는지 결정 기록 | 추측이 아니라 A/B 테스트로 정했습니다 |
| `AGENT_BRIEF_V3.md` | 짜임·금지 문자열 규칙 (§4·§5·§6 유효) | 살아있음 |
| `AGENT_BRIEF_V6.md` | **현행 디자인 규격** — toss.im 실측 토큰 | 살아있음. V4·V5 는 폐기(`_보관/`) |
| `DESIGN_SPEC.md` | 클라이언트 요구 원문 + **서버 실측 사실** | §1 에 요구사항, §2 에 SSH 조사 결과와 함정 4건 |
| `PLAN.md` | 최초 구현 계획 (40KB) | 역사 기록 |
| `ag-college.html` `part2-quota.html` `ag-ax.html` `ag-faculty.html` | **AG 시안 4장 — 주력 산출물** | |
| `a-college.html` | **A 시안 1장** | |
| `index.html` | 시안 색인 (클라이언트가 보는 목록) | 카드가 JS 배열(`P1`…`P8`)로 생성됩니다 |
| `sitemap.html` | 페이지 구성 문서 | |
| `{a~z}-college.html` `{a~t}-hub.html` `part3-ax.html` 등 | **반려·이전 시안 40여 장** | 옛 수치 있음. **색인이 링크하므로 옮기지 마십시오** |
| `assets/` | `ag-motion.js`(AG) · `motion.js`(A) · `base.css` · `video/` · `logo/` | |
| `deploy/ag/` `deploy/a/` | **실제 배포된 것과 같은 파일** | §3 |
| `check.py` | 전수 검증기 | §3 |
| `_보관/` | 실험 하네스·폐기 브리프·외부 사이트 사본 | `_보관/README.md` 참조 |

---

## 3. 실행 · 검증 · 배포

### 로컬에서 보기

정적 파일뿐이라 서버만 있으면 됩니다. **파일을 더블클릭하지 마십시오** — `file://` 에서는
일부 스크롤 애니메이션과 영상 자동재생이 동작하지 않습니다.

```powershell
cd D:\_인수인계_20260907\10_프로젝트\부산대학교_AI대학_홈페이지\원본\PNU_AI\시안_20260822
python -m http.server 3777
# http://localhost:3777/index.html  ← 색인부터
```

포트는 아무거나 좋습니다(작업 중에는 3777 을 썼습니다).

### 검증

```powershell
cd ...\시안_20260822
python check.py
# 통과 시: PASS — 56 pages (9 core + 47 optional), 5 assets   / 종료코드 0
```

`check.py` 는 금지 수치(`377` `114명` …) · 금지 서체 · 무근거 주장 · 자산 경로를 전수 검사합니다.
**페이지를 고쳤으면 반드시 돌리십시오.** 옛 수치가 되살아나는 사고가 실제로 여러 번 있었습니다.

파일명에 따라 규칙이 자동으로 갈립니다 — `ag-*` · `part2-*` 같은 최신 페이지는 V7 규칙(`424` `79` `35` `27` 필수,
`114명` 금지), 나머지는 옛 규칙을 받습니다.

### 배포 (Netlify)

의존성: **Netlify CLI** (`npm i -g netlify-cli`), 로그인된 계정.

```powershell
# AG 시안
cd ...\시안_20260822\deploy\ag
netlify deploy --prod --dir=. --site=bba6e5d0-898e-4e5a-9d07-7add11230754

# A 시안
cd ..\a
netlify deploy --prod --dir=. --site=0e73be81-bebf-4def-a90a-8acbd6808065
```

| 사이트 | site-id | URL |
|---|---|---|
| `pnu-ai-college-ag` | `bba6e5d0-898e-4e5a-9d07-7add11230754` | https://pnu-ai-college-ag.netlify.app |
| `pnu-ai-college-a` | `0e73be81-bebf-4def-a90a-8acbd6808065` | https://pnu-ai-college-a.netlify.app |

같은 계정에 `pnu-ai-college` · `pnu-ai-college-279` · `pnu-ai-college-s31`(→ `pnug.kodekorea.kr`) 도 있습니다.
**Netlify 팀 슬러그는 `kgpolsm-ai`.** 개인 계정으로 보입니다(**추정**) — 조직 계정으로 이관이 필요한지
확인하십시오. **자격증명은 이 폴더에 없습니다** → `..\..\..\30_계정_API키\` 를 보십시오.

> ⚠️ `deploy/ag` · `deploy/a` 는 **손으로 추린 스테이징 폴더**입니다. 생성 스크립트가 남아 있지 않습니다
> (**추정** — 작업 당시 임시 스크립트로 만들고 저장하지 않은 것으로 보입니다).
> 소스를 고치면 `deploy/` 쪽에도 **직접 복사**해야 합니다. 빠뜨리기 쉬운 것:
> `assets/ag-motion.js` · `assets/video/facade.mp4` · `assets/logo/pnu-signature.png`.
> `_headers` · `robots.txt` · `.netlify/netlify.toml`(noindex 헤더)은 **배포 폴더에만 있고 소스에는 없습니다.**

### 최종 목표 배포처 (아직 안 함)

`arise-ai.pusan.ac.kr` 의 React 앱에 **정식 라우트로 통합**하는 것이 확정된 방향입니다.

- 저장소 `mslopgk/arise-pnu-ai` — 업무PC 경로 `C:\Users\user\Desktop\pnu_grad\pnu_grad`
- `frontend/src/variants/` 아래 페이지 추가 + `App.jsx` 에 `<Route>` 등록. **main 직접 작업 금지, feature 브랜치**
- **CDN 금지 · 전부 로컬 번들** — 그 저장소의 `DEPLOY.md` 가 폐쇄망 동작을 보장하고 있어 깨면 안 됩니다.
  GSAP 은 이미 번들돼 있고, 지금 시안이 쓰는 **Pretendard·GSAP CDN `<script>` 는 npm 으로 바꿔야 합니다**
- 서버 `pnu-grad` · Ubuntu 24.04 · Compose 프로젝트 `arise` @ `/home/ubuntu/arise-stack`
- 자세한 것은 `시안_20260822/DESIGN_SPEC.md` §2-1

---

## 4. 상대 기관 · 담당자 · 약속

| 항목 | 내용 |
|---|---|
| 발주 | 부산대학교 **AI 거점대학육성사업단** |
| 담당 교수 | **김종덕** 교수 (정보컴퓨터공학부 · 지능통신연구실 · Digital-X AIoT 연구센터장). 연락처는 이 폴더에 없습니다 |
| 최초 마감 | **2026-08-24(월) 본부에 홍보 페이지 주소 제출.** 이미 지났습니다 — 실제로 제출됐는지, 어떤 주소로 제출됐는지 **확인되지 않았습니다(추정: 미확인)** |
| 상위 문서 | `..\..\20260825_AI대학홈페이지_검토요청.hwpx` (프로젝트 폴더 최상단) |

### 합의된 내용 (클라이언트 메일 2건 — 원문은 `DESIGN_SPEC.md` §1.1 · `FACTS_V7.md` 머리말)

1. `ai.pusan.ac.kr` 은 **보류** (AI대학원과 협의 미완). 대신 **기존 `arise-ai.pusan.ac.kr` 을 수정**해 대응
2. 진입 3개를 **①AI 대학 ②A.U.R.A ③Google for Education** 으로 재구성. 기존 ③「학·석사 연계과정 신청」은 **숨김**
3. **2027년 3월 출범을 강조하는 좋은 문장**이 필요 (일자는 특정하지 말 것 — 원자료가 「3월」까지만 확정)
4. 내부 페이지에 **ADP+X 구조**와 **ADP 코어 학부/학과** 소개 — **학과 기본 정보 + 소속 교원 정보 포함**
5. **세부 전공별 정원**과 **8개 분야 17개 AX융합전공**을 반드시 넣을 것

### 대표(사내) 지시 — 우선순위 최상

- **「대학 공식 홈페이지 느낌을 절대 살리지 말 것」**
- **기존 arise-ai 디자인을 참고·의도적 반대·유사하게 하는 것 전부 금지**
- **긴 스크롤 싫어함** — 페이지당 짧게
- **「글 이정도로 없어도 되나 싶을 정도」 · 거의 다 인포그래픽** ← 「젤 중요함」이라고 못박은 항목
- 최종적으로 **AG 와 A 만 쓴다**

---

## 5. 함정과 주의사항

### 자료·수치

1. **`FACTS_V7.md` 말고 다른 수치를 믿지 마십시오.** 이 프로젝트에서 가장 자주 터진 사고입니다.
   특히 **「데이터사이언스학부 114명」은 공식자료에 없는 수치**입니다 → 올바른 값은 **데이터사이언스부 79 + 통계학과 35**.
   `check.py` 가 막고 있지만, SVG 좌표(`M296,114`)나 속성값에 숨어 통과한 적이 실제로 있습니다.
2. **X-모빌리티융합학부 · 바이오메디컬공학과는 공과대학**입니다. AI대학 정원에 합산하지 마십시오.
3. **출범 일자를 특정하지 마십시오.** 원자료가 「2027년 3월」까지만 확정합니다.
4. **각주 「정원·편제·조직은 승인 전 계획값입니다」는 필수**입니다.
5. `_보관/s31/` 의 `377명`, `_보관/조사자료/` 의 크롤링 값은 전부 낡았거나 2차 자료입니다.

### 디자인 판단

6. **`기획/디자인_디렉션.md` 은 이 프로젝트 문서가 아닙니다.** 스스로 「기준 시안: studio-site — ALTITUDE
   시네마틱 스튜디오 랜딩」이라고 밝히는 **다른 프로젝트 문서**입니다. 여기 적힌 「발광 입자」·「영상이 화면 95%」를
   이 프로젝트에 가져다 쓴 적이 있고, **요청받은 적 없는 방향이라 반려**됐습니다
   (그 결과물이 `_보관/폐기_브리프/AGENT_BRIEF_V5.md` 와 시안 AD·AE·AF).
7. **「초딩같다」의 원인은 두 가지뿐**입니다 — ⓐ 고채도 색을 항목마다 균등 배분 ⓑ 회색빛 저대비.
   「밀도를 높이면 / 세리프를 쓰면 / 형태를 데이터에서 파생시키면 진지해진다」는 **A/B 테스트에서 전부 기각**됐습니다.
   추측하지 말고 `_보관/실험_하네스/_taste.html` 로 다시 재십시오.
8. **액센트 색은 파랑 하나(`#3182F6`)뿐**입니다. 항목별로 색을 나눠 주지 마십시오.
   그리고 **`#3182F6` 은 흰 바탕에서 3.71:1** 이라 작은 글씨에 쓰면 AA 미달입니다 → 13px 이하는 `#155BBF`(6.40:1).
9. 같은 꼴의 카드 그리드를 연달아 놓으면 **「같은 느낌 반복돼서 별로」** 라는 반려를 받습니다.
   AG 는 그래서 네 구획이 전부 다른 꼴입니다 — 영상 → 가로 띠 → 목록+막대 → 큰 숫자.

### 코드

10. **부드러운 스크롤(Lenis)은 일부러 뺐습니다.** Lenis 는 현재 위치를 목표까지 서서히 옮기는 방식이라
    **「부드러움의 양 = 지연의 양」** 입니다. `duration 1.05`(≈1000ms) → `lerp 0.14`(≈300ms) → `lerp 0.28`(≈120ms)
    세 번 다 **「한 발 늦다」** 는 지적을 받았습니다. 지금은 **네이티브 스크롤 + GSAP `scrub: 0.9/0.6/0.5`** 입니다 —
    입력은 즉시, 애니메이션만 부드럽게. **다시 넣지 마십시오.** 경위는 `assets/ag-motion.js` 머리주석에 있습니다.
    (**A 시안에는 Lenis 가 아직 들어 있습니다.** 최초 지시의 4개 라이브러리를 다 쓴 버전이라 그대로 뒀습니다.)
11. **`scrub: true` 는 쓰지 마십시오.** 스크롤을 즉시 따라가서 휠의 100px 계단이 애니메이션에 그대로 찍힙니다.
    숫자(`0.5`~`1.4`)를 주면 애니메이션이 그 시간 동안 따라잡으며 계단이 메워집니다.
12. **`gsap.from()` 금지 → `fromTo()` + `immediateRender:false`.** CSS 가 이미 최종 상태를 갖고 있을 때
    `from()` 을 쓰면 시작값이 끝값으로 굳어 **콘텐츠가 사라집니다.** 실제로 터진 적 있습니다.
13. **`body{overflow-x:hidden}` 은 자손의 `position:sticky` 를 조용히 죽입니다** (body 가 스크롤 컨테이너가 됨).
    `overflow-x:clip` 을 쓰십시오. 탭바가 안 붙던 버그의 원인이 이것이었습니다.
14. **flex 아이템의 `padding` 은 여유공간 분배 *전에* 빠져나갑니다.** 비례 띠(`flex:214` 등)에
    `padding` 을 주면 작은 구간이 부풀어 비율이 깨집니다(실측 48.8px 오차). 안쪽 span 에 주십시오.
15. **flex 아이템에 `margin:0 auto` 를 주면 `stretch` 가 꺼져** 폭이 콘텐츠 크기로 붕괴합니다.
    `width:100%; max-width:…; margin-inline:auto` 를 쓰십시오.
16. **콘텐츠를 JS 로 드러내는 구조를 만들지 마십시오.** 기본 상태가 곧 최종 상태여야 합니다 —
    스크립트가 안 뜨면 페이지가 백지가 됩니다. `Ctrl+F` 로 「424」가 찾아지는지로 확인하십시오.
17. 한글이 든 `.ps1` 은 **UTF-8 BOM** 으로 저장해야 합니다. 아니면 CP949 로 읽혀 파서 오류가 납니다.

### 서버 (원문은 `DESIGN_SPEC.md` §2-1)

18. **와일드카드 인증서 `*.pusan.ac.kr` 이 2027-01-02 에 만료**됩니다 — AI대학 출범(2027-03)보다 먼저입니다.
    **갱신 주체를 확인해야 합니다.**
19. 호스트의 `arise-stack/web/dist` 는 **stale** 입니다. 여기를 고쳐도 사이트는 안 바뀝니다.
    서버에는 git 저장소도 React 소스도 없고 빌드된 이미지만 있습니다 — **소스 작업은 로컬 저장소에서**.
20. 방화벽이 Wi-Fi 공인 IP 기반이라 `~/.ssh/config` 의 `BindAddress` 를 접속할 때마다 갱신해야 했습니다.

---

## 6. 다음에 해야 할 일

### 즉시 — 라이브 URL 에 있는 버그

1. **`part2-quota.html` 푸터의 링크 2개가 404 입니다** (소스·배포본 양쪽에서 확인함).
   - `href="part1-college.html"` → **그런 파일이 없습니다** (Part 1 을 만들다 중단)
   - `href="part3-ax.html"` → 소스엔 있지만 **`deploy/ag/` 에 없어** 배포본에서 404
   - 고칠 곳: `시안_20260822/part2-quota.html:452-453` 과 `deploy/ag/part2-quota.html` 의 같은 자리.
     상단 탭바(`:200-203`)는 `ag-college / part2-quota / ag-ax / ag-faculty` 로 올바르므로
     **푸터를 탭바와 같은 대상으로 맞추면 됩니다.**

2. **색인·사이트맵의 옛 수치.** `index.html` 푸터가 아직 「424명(A 214 / **D 114** / P 69 / X 27)」 이라고 적습니다.
   클라이언트가 보는 첫 화면입니다. `sitemap.html` 도 같습니다.
   → `FACTS_V7.md` 기준으로 정정 (`A 214 / D 79 + 통계 35 / P 69 / X 27`).

3. **AG 하위 3장이 색인에서 안 보입니다.** `index.html` 의 `P8` 배열은 `ag-college.html` 만 링크합니다.
   `part2-quota` · `ag-ax` · `ag-faculty` 로 들어갈 길이 색인에 없습니다.

### 확인 대기

4. 대표가 마지막으로 확인 중이던 것: **AG 히어로에서 천천히 스크롤할 때 영상 확대가 계단식으로 튀는지.**
   여전히 튀면 `assets/ag-motion.js` 의 `scrub: 0.9` 를 **1.4 정도로 올리면** 됩니다.
5. **A 시안의 Lenis 도 뺄지** — 아직 답을 못 받았습니다. AG 와 같은 지적을 받으면 §5-10 과 같은 방식으로 제거.
6. 클라이언트에게 두 URL 을 실제로 전달했는지, 어느 쪽을 골랐는지 **확인되지 않았습니다(추정: 미확인)**.

### 미완성

7. **Part 1(AI대학 개요 페이지)** — 「일단 하지 말자」로 중단됐습니다. `part2-quota.html` 이 이미 링크를
   걸어 두었으므로(위 1번) 만들거나 링크를 지우거나 둘 중 하나는 해야 합니다.
8. **Part 3(AX융합전공)의 내용 채우기는 취소**됐습니다. AG 쪽 `ag-ax.html` 이 그 역할을 합니다.
   `part3-ax.html` 은 옛 골격만 남은 상태입니다.
9. **반려 시안 40여 장의 옛 수치 정정** — 안 했습니다. 시안으로만 남길 거면 안 해도 되지만,
   **색인을 클라이언트에게 계속 보여줄 거라면 최소한 겉으로 보이는 숫자는 맞춰야 합니다.**
10. **모바일 대응** · **학부·학과 상세 페이지** — 방향 확정 후 진행하기로 한 범위 밖 항목입니다.

### 정리

11. **git 이 커밋되지 않은 상태**입니다. `시안_20260822/` 의 마지막 커밋은 `a007201` 이고
    그 뒤 작업(AG 4장 완성 · Lenis 제거 · `deploy/` · `check.py` 수정)이 전부 미커밋입니다.
    **원격 저장소가 없습니다** (`git remote -v` 비어 있음).
    이번 인수인계 정리로 폴더를 옮긴 것도 `git status` 에 삭제로 잡히니,
    커밋 전에 §2 지도를 보고 의도된 이동인지 확인하십시오.
12. `_보관/_ref/` 는 **다른 사람이 만든 사이트의 사본(126MB)** 입니다. 최종 납품물에 넣지 마십시오.

---

## 7. 확실하지 않은 것 (추정)

- Netlify 팀 `kgpolsm-ai` 의 **소유·이관 필요 여부** — 개인 계정으로 보이나 확인 안 됨
- **2026-08-24 본부 제출이 실제로 이루어졌는지**, 어떤 주소로 제출됐는지
- `deploy/ag` · `deploy/a` 스테이징 폴더의 **생성 스크립트가 저장되지 않았다**는 판단 (폴더 안에서 찾지 못했습니다)
- `_보관/_ref/` 원본(업무PC `E:\AIWeb-site`)의 **작성자와 사용 허락 범위**
- 클라이언트가 **두 시안 중 어느 쪽을 골랐는지**
