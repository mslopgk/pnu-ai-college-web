# 부산대학교 AI대학 홈페이지 — 인수인계

작성 2026-09-08 · 대상 폴더 `D:\_인수인계_20260907\10_프로젝트\부산대학교_AI대학_홈페이지\원본\AIWeb-site`
원본 작업 폴더는 `D:\AIWeb-site` 였습니다. 이 폴더는 그 독립 복사본입니다(하드링크 아님, 2026-08-27 시점 스냅샷).

> 이 문서는 **코드를 봐도 알 수 없는 것**만 담았습니다. 무엇이 왜 그렇게 되어 있는지, 무엇을 시도했다 실패했는지, 무엇이 아직 결정되지 않았는지가 핵심입니다.
> 확실하지 않은 것은 **(추정)** 으로 표시했습니다. 표시 없는 것은 코드·git 이력·실측으로 확인한 사실입니다.

---

## 1. 무엇인가 / 어디까지 됐나

### 프로젝트

2027년 3월 출범 예정인 **부산대학교 AI대학**의 소개 홈페이지입니다.
빌드 도구가 없는 **순수 정적 사이트**입니다 — `package.json`도 `node_modules`도 없습니다. HTML 1개 + JS 2개 + CSS 1개 + 정적 자산이 전부입니다.

- 운영 주소: `https://arise-ai.pusan.ac.kr/ai-college`
- 프리뷰: `https://pnu-ai-college.pages.dev` (Cloudflare Pages)
- 저장소: `https://github.com/mslopgk/pnu-ai-college-web.git` (`main` 브랜치, 커밋 32개, 전량 이지호 단독 작성)

구조는 **인트로 스크럽 영상 → 인덱스 오버레이 → 5개 상세 섹션**의 단일 페이지입니다. 스크롤 위치로 영상 프레임을 스크럽하고, 섹션 전환은 해시 라우팅(`#detail/...`)으로 처리합니다.

섹션 5개 (`app.js` 최상단 `sections` 배열이 원천):
AI대학 소개 · AI대학 조직체계 · 신입생모집 · AI대학 특화 프로그램 · 함께하는 교수진들

### 동작하는 것

| 기능 | 상태 |
|---|---|
| 인트로 스크럽 영상 + 포스터 폴백 | 동작. 영상 없으면 첫 프레임 이미지 표시 |
| 해시 라우팅 / 뒤로가기 | 동작 |
| 5개 섹션 상세 + GNB 드롭다운 | 동작 (규격은 `DETAIL_GNB_SPEC.md`) |
| 교수 탐색 (60명) | 동작. 사진 **60/60 완비** |
| 사이트 내 검색 | 동작 |
| 학과·기관 공식 홈페이지 링크 | 동작 (`app.js`의 `DEPARTMENT_LINKS`, `ORG_NODE_LINKS`) |
| 연구실 바로가기 바 (푸터형 접이식) | 동작. **39명분 게재** |
| 정원 표기 | 총 424명 (AI컴퓨터공학부 214 / 데이터사이언스학부·통계학과 114 / 산업공학부 69 / AX융합학부 27). 합계 일치 검증 완료 |

### 안 되는 것 / 미완

| 항목 | 내용 |
|---|---|
| 연구실 링크 21명 미게재 | 교수 60명 중 39명만 게재. 근거 부족 14명 + 링크 없음 4명 + 공식 링크가 죽은 3명. 3절 참조 |
| 부산대 공식 도메인 게재 승인 | **미승인.** 현재 Cloudflare 프리뷰까지만 올라가 있음. 5절 참조 |
| 교수 개별 확인 요청 | **미발송.** 발송 경로 미결정. 5절 참조 |
| `fonts/`·`docs/` 미커밋 | git에 없음. **클론만 하면 사이트가 깨짐.** 4절 최상단 참조 |

---

## 2. 파일·폴더 지도

정리 원칙: 핵심 소스·설정·문서는 루트에 드러냈고, 재생성 가능한 산출물·캐시는 `_보관/`으로 옮겼습니다. **삭제한 파일은 없습니다** (정리 전후 418개 동일, git 제외).

### 루트 — 여기만 보면 됩니다

| 항목 | 크기 | 무엇인가 / 왜 여기 있나 |
|---|---|---|
| `index.html` | 4KB | 유일한 HTML. 섹션 골격과 `?v=` 캐시 버스팅 쿼리가 여기 있음 |
| `app.js` | 78KB | 전체 로직. 라우팅·스크럽·섹션 렌더·검색·연구실 링크 바. **단일 파일, 모듈 분리 없음** |
| `faculty-data.js` | 32KB | 교수 60명 데이터. `window.FACULTY_DATA` 전역 배열. 필드: `college, department, major, rank, name, specialty, career, aiCareer, image, isRepresentative`, 그리고 39명에만 `lab{name,url}` |
| `styles.css` | 102KB | 전체 스타일. 역시 단일 파일 |
| `assets/` | 4.8MB · 71개 | 이미지·영상·아이콘. `faculty/` 안에 교수 사진 60장(해시 파일명 `.webp`). 인트로 영상 `higgsfield-pnu-particles.mp4`, 포스터 `higgsfield-pnu-poster.webp`. **git에 추적됨** |
| `fonts/` | 3.8MB · 126개 | Noto Sans KR 가변폰트 로컬 번들(`files/` 안 woff2 124개 + `noto-sans-kr.css` + LICENSE). 폐쇄망 대응으로 Google Fonts CDN 대신 로컬 번들. ⚠️ **git 미추적** |
| `docs/` | 6.2MB · 8개 | 발주처 원천 자료 + 조사 산출물. 아래 별표. ⚠️ **git 미추적** |
| `netlify.toml` | 256B | Netlify 설정. `publish = "."` (루트를 그대로 배포), 빌드 명령 없음 |
| `.gitignore` | 66B | `.netlify/ .DS_Store Thumbs.db node_modules/ tmp/ dist/ .wrangler/` |
| `README.md` | 451B | 로컬 실행법 한 줄. 내용이 얇음 |
| `DETAIL_GNB_SPEC.md` | 6.6KB | 상세영역 GNB 규격서 11장. 디자인 토큰·레이아웃·드롭다운·스크롤 동작·접근성. **GNB 손댈 때 먼저 읽을 문서** |
| `_보관/` | 8.8MB | 아래 별표 |
| `.git/` | 72MB | 커밋 32개. 폴더 용량의 대부분이 이것 |

### `docs/` 안

| 파일 | 출처 / 용도 |
|---|---|
| `AI거점대학 제안 내용 중 AI 대학 관련 부분.hwpx` (4.5MB) | 발주처 원천 자료. 사이트 내용의 근거 문서 |
| `AI거점대학 융합전공 및 교육관련 내용.hwpx` | 발주처 원천 자료. 융합전공·교육체계 |
| `page 4. AI대학.pdf` | 발주처 원천 자료 |
| `260824-294-부산대 패키지 지원 관련 신설학과 신입생 모집-컨펌용.hwp` | 신입생 모집 관련 컨펌용 문서 |
| `AI대학교원정보.xlsx` | **교수 데이터의 원천.** `faculty-data.js`는 이걸 옮긴 것. 세부전공 대조에 재사용됨(커밋 `091a70f`) |
| `정원조정_시각화.html` | 정원 424명 산출 검토용으로 만든 단독 HTML (추정 — 커밋 이력에 근거 없음, 파일 성격으로 판단) |
| `faculty-lab-sites.md` (31KB) | **연구실 링크 60명 전수 조사 문서.** 게재 근거 분류(A/B/C), 죽은 링크 판정, 검증 함정까지 전부 여기 있음. 3절의 원천 |
| `pnu-ihis-site-registry.tsv` | 부산대 홈페이지 통합시스템(IHIS) 공식 등록 사이트 223건 전수 덤프 |

### `_보관/` 안

`빌드산출물_dist/`(옛 `dist/`) · `배포캐시_netlify/`(옛 `.netlify/`) · `배포캐시_wrangler/`(옛 `.wrangler/`)
상세는 [`_보관/_보관_설명.md`](_보관/_보관_설명.md). **`빌드산출물_dist/_headers`는 루트에 대응 파일이 없는 유일한 파일이니 그 문서를 꼭 보십시오.**

---

## 3. 연구실 링크 39개 — 게재 근거 분류

이 프로젝트에서 가장 품이 많이 든 부분이고, **잘못 건드리면 대외 문제가 되는 부분**입니다.
전수 조사 결과는 `docs/faculty-lab-sites.md`에 있고, 요지는 이렇습니다.

교수 60명의 연구실 URL을 **"부산대 공식 페이지가 그 주소를 가리키는가"** 기준으로 나눴습니다. 오기가 났을 때 "학부 공식 페이지에 그렇게 적혀 있었다"고 말할 수 있는지가 갈림입니다.

| 구분 | 인원 | 게재 | 방어 논리 |
|---|---|---|---|
| **A-1** 공식 게재 주소 그대로 | 30 | 27 | 완전한 명분. (죽은 링크 3명은 제외하여 게재) |
| **A-2** 같은 호스트, 경로만 보정 | 10 | 10 | 공식이 가리킨 도메인 그대로, 루트가 스텁이라 실경로만 맞춤 |
| **A-2b** 공식 링크의 리다이렉트 목적지 | 2 | 2 | 공식 링크를 브라우저로 누르면 자동으로 그 주소에 도착 (이환희·백윤주) |
| **B** 검색으로 찾음 | 14 | 0 | **방어 논리 없음.** 학부 페이지에 링크 자체가 없음 |
| **C** URL 자체가 없음 | 4 | 0 | 해당 없음 (이화세·정재희·이동혁·유철희) |

게재 39 = A-1 27 + A-2 10 + A-2b 2. 미게재 21 = B 14 + C 4 + 죽은 링크 3.

**미게재 B그룹 14명**: 안성용 감진규 권동현 박진선 김원석 김태운 조준수 전상률 손준영 박영진 최동희 권용인 김철기 김태완
**죽은 링크 3명**: 이기준(서버 다운) · 김정구(중지된 사이트) · 탁성우(블로그 삭제) — 공식 페이지를 옮긴 것이라 책임은 없으나 이용자에게는 깨진 링크이므로 게재하지 않았습니다.

패널 하단에 `position:sticky`로 **"각 학부·학과 공식 페이지 게재 기준 · 최종 확인 2026-08-27"** 문구가 고정 노출됩니다. 이게 실질적 방어선이니 지우지 마십시오.

### 공식 페이지 쪽이 틀린 2건 — 정정 요청 대상

- **손준영 교수**: 정보컴퓨터공학부 프로필이 `infosec.pusan.ac.kr`을 가리키는데 그건 **김호원 교수 연구실**입니다. 실제는 `accs.pusan.ac.kr`
- **감진규 교수**: 학부 게시 `bic.pnu.ac.kr`은 도메인 매핑이 삭제되어 죽었습니다. 실제는 `jkgahm.github.io`

두 분은 B그룹(공식 근거 없음)이라 미게재 상태입니다. 확인 요청을 보낼 때 학부 페이지 정정 요청을 함께 넣는 게 효율적입니다.

---

## 4. 실행 · 빌드 · 배포

### 로컬 실행

빌드 단계가 없습니다. 정적 서버만 띄우면 됩니다.

```powershell
cd <프로젝트 폴더>
python -m http.server 4173
# http://localhost:4173
```

포트 4173은 `README.md`에 적힌 관례값이고 코드에 하드코딩된 곳은 없습니다. 아무 포트나 됩니다.
`index.html`을 파일로 직접 열어도 대체로 뜨지만, `file://`에서는 영상·폰트 로딩이 브라우저 정책에 걸릴 수 있으니 서버로 띄우십시오.

**필요한 것**: 정적 서버 하나. Node·npm·환경변수·API 키 **전부 불필요**합니다.

### ⚠️ 클론하면 사이트가 깨집니다 — 가장 중요한 함정

`fonts/`와 `docs/`가 **git에 커밋되어 있지 않습니다**(추적 파일 79개 = 루트 설정·소스 8개 + `assets/` 71개). `.gitignore`에 적힌 것도 아니고, 그냥 커밋되지 않은 상태입니다.

`index.html`이 `./fonts/noto-sans-kr.css`를 **하드 의존**하므로, 새로 `git clone`만 하면 한글 폰트가 전부 시스템 폰트로 폴백되어 레이아웃이 무너집니다. 폐쇄망 대응으로 CDN을 안 쓰기 때문에 대체 경로도 없습니다.

→ **이 인수인계 폴더의 `fonts/`·`docs/`를 반드시 함께 보존하십시오.** git만 믿으면 안 됩니다.
→ 후임의 첫 판단 사항: 이 두 폴더를 커밋할지 여부. `fonts/`(3.8MB, 124개)는 런타임 필수라 커밋하는 쪽이 맞다고 봅니다. `docs/`는 발주처 원천 문서(hwpx·pdf·xlsx)라 공개 저장소 커밋이 적절한지 별도 판단이 필요합니다.

### ⚠️ 캐시 버스팅은 수동입니다

`index.html`이 세 파일을 `?v=날짜-순번`으로 참조합니다. 현재 값은 **`?v=20260827-1`** 입니다.

```html
<link rel="stylesheet" href="./styles.css?v=20260827-1" />
<script src="./faculty-data.js?v=20260827-1"></script>
<script src="./app.js?v=20260827-1"></script>
```

**`app.js`·`styles.css`·`faculty-data.js`를 고치면 이 값을 반드시 올리십시오.** 자동화가 없습니다.
안 올리면 기존 방문자 브라우저에 옛 파일이 그대로 나갑니다. 부산대 서버로 올릴 때도 이 값이 반영되어야 합니다.

### 배포

배포 설정이 **두 갈래**로 남아 있습니다. 실제로 최근 쓰인 것은 Cloudflare Pages입니다.

| 대상 | 설정 파일 | 상태 |
|---|---|---|
| Cloudflare Pages | `_보관/빌드산출물_dist/_headers` | 프리뷰 `pnu-ai-college.pages.dev` 운용 중 |
| Netlify | `netlify.toml` (`publish = "."`) | 설정은 남아 있음. 최근 사용 여부 불명 (추정: 초기에 쓰다 Cloudflare로 옮긴 것으로 보임) |
| 부산대 서버 (`arise-ai.pusan.ac.kr`) | 없음 | **절차 문서 없음.** 아래 참조 |

`dist/` 만드는 방법: 루트의 `app.js faculty-data.js index.html styles.css assets/ fonts/`를 복사하고 `_headers`를 넣습니다. 스크립트가 없어 수동입니다(md5 대조로 단순 복사임을 확인).

**부산대 서버 배포 절차는 이 프로젝트에 문서화되어 있지 않습니다.** 커밋·설정 어디에도 없습니다.
(추정) 사이트 담당자나 다른 작업 세션이 별도로 수행한 것으로 보이며, 인수인계 시점에 **누가 담당인지 확정되지 않았습니다.** 후임이 가장 먼저 확인해야 할 항목입니다.

### 자격증명 위치 — 값은 여기 쓰지 않았습니다

| 무엇 | 어디에 |
|---|---|
| Cloudflare 계정 id·이름, Pages 프로젝트명 | `_보관/배포캐시_wrangler/cache/wrangler-account.json`, `.../cache/pages.json` |
| Netlify siteId | `_보관/배포캐시_netlify/state.json` |
| Cloudflare 배포 권한 | 파일이 아니라 **`wrangler login` 세션**에 있음. 새 PC에서는 재로그인 필요 |
| Netlify 배포 권한 | **`netlify link` / `netlify login` 세션** |
| GitHub 푸시 권한 | 저장소 `mslopgk/pnu-ai-college-web`. 계정 이전·협업자 추가가 필요할 수 있음 (추정 — 소유 계정이 개인 계정으로 보임) |

위 캐시 파일들에서 **토큰·비밀키는 확인되지 않았습니다**(계정 식별자만). 그래도 외부 공개는 피하십시오.

---

## 5. 상대 기관 · 담당자 · 미결 사항

### 확인된 것

- **대상 기관**: 부산대학교 AI대학 (2027년 3월 출범 예정)
- **사이트 공개 연락처**: `ai@pusan.ac.kr` (푸터에 표기)
- **검토 반영 이력**: 부산대 측에서 검토서를 받아 반영한 커밋이 있습니다 — `a3bc033 홈페이지 검토 요청(20260825) 수정 필요 항목 반영`, `f9d4f05 검토서 [확인 필요] 항목 중 문서와 어긋나던 4건 정정`. 즉 **발주처 검토 사이클이 실제로 돌고 있었습니다.**
- 그 과정에서 반영된 개별 지시: AI대학 전화번호 삭제(`3c619e3`), 인트로 영상 엠블럼 연도 1965→1946(`6fabda6`), 스마트가전공학과(계약학과) 모집단위 삭제(`92d104b`), AX융합학부 정원 27명 반영(`4526b36`)

### 알 수 없는 것 — 후임이 확인해야 함

- **계약 관계·발주 형태·대금** — 이 프로젝트 파일과 대화 이력에 근거가 없습니다. 모릅니다.
- **사이트 오픈 마감일** — 학교 출범이 2027년 3월이라는 것만 확인됩니다. 사이트 자체의 마감일은 모릅니다.
- **부산대 측 실무 담당자 성명·연락처** — `ai@pusan.ac.kr` 외에는 확인된 것이 없습니다.

### 미결 2건 — 이 프로젝트에서 결론이 안 난 것

**① 미게재 14명 확인 요청을 누구에게 보낼지**

B그룹 14명에게 연구실 주소 확인을 받으면 게재분이 39 → 53명이 됩니다. 그런데 **확인 요청을 어느 경로로 보낼지가 결정되지 않았습니다.**
후보로 거론된 것은 (가) 김종덕 교수님 경유, (나) 각 학부 행정실 두 가지입니다. (추정 — 김종덕 교수님이 창구로 언급된 근거는 대화 중 후보 제시뿐이고, 본인 동의를 받은 바 없습니다.)

주의: 같은 시기 **다른 작업 세션(`aiweb-site-24`)도 동일한 질문을 자기 담당자에게 올려둔 상태**입니다. 양쪽이 따로 보내면 **교수님들께 중복 연락이 갑니다.** "발송은 한 쪽이 맡고 다른 쪽은 결과만 받는다"는 원칙까지만 합의되었고, 어느 쪽이 맡을지는 미정입니다.

**② 부산대 공식 도메인 게재 승인**

연구실 링크 39개는 GitHub·Cloudflare 프리뷰까지 올라가 있지만 **부산대 공식 도메인 게재는 승인을 받지 않았습니다.**
"승인 전에는 공식 도메인에 올리지 않는다"까지만 합의되어 있습니다. 되돌리려면 커밋 하나(`0d19dd8` 계열)를 되돌리면 됩니다.

---

## 6. 함정과 주의사항

### 데이터 관련

**학과명 일괄치환 금지 — 실제로 회귀를 냈던 부분입니다.**
`정보컴퓨터공학부`와 `AI컴퓨터공학부`는 **별개 학부**입니다. 이름이 비슷해서 일괄 치환하면 데이터가 섞입니다. 커밋 `091a70f`(Codex 교차검증 반영: 일괄치환 회귀 수정)와 `b3f5376`(학과명 원상복구)이 그 흔적입니다. 학과명을 만질 때는 `docs/AI대학교원정보.xlsx`와 재대조하십시오.

**김민우 교수가 2명입니다.**
데이터사이언스학부(부교수, 의료영상)와 통계학과(조교수, 베이지안·공간통계)에 동명이인이 있습니다. 교수를 이름으로만 조회하면 잘못 매칭됩니다. **`(학부, 이름)` 복합키를 쓰십시오.** 연구실 링크 병합도 이 방식으로 되어 있습니다.

**교수 데이터의 원천은 `docs/AI대학교원정보.xlsx`입니다.** `faculty-data.js`를 직접 고치기 전에 원천을 확인하십시오.

### 성능 관련 — 되돌리면 스크롤이 끊깁니다

스크롤 성능은 여러 커밋에 걸쳐 고친 것이고, 각각 이유가 있습니다. 리팩터링하면서 무심히 되돌리지 마십시오.

- `130ce61` 파티클 애니메이션 **1031개로 축소 + 곡선 좌표 캐시** — 이걸 늘리면 스크롤 끊김이 재발합니다
- `app.js` 상단의 `__cssVarGuard` — CSS 변수에 **같은 값을 다시 쓰면 브라우저가 하위 트리 스타일을 재계산**하므로, 값이 바뀔 때만 통과시키는 몽키패치입니다. 스크롤 애니메이션이 매 프레임 같은 값을 쓰기 때문에 필요합니다
- `95e3202` 프로그램 `draw()` 죽은 계산 제거 · `b93769f` 조직체계 단계 내 대기 68%→20%
- 연구실 링크 목록은 **첫 열기 때 생성**합니다. 항상 들고 있으면 노드가 260여 개 늘어납니다

### 링크 검증 관련 — 부산대 사이트 특유의 함정 4가지

`docs/faculty-lab-sites.md`의 「링크 재검증 시 주의」에 자세히 있습니다. 요약하면:

1. **`*.pusan.ac.kr` 루트 도메인은 대부분 "site move" JS 스텁입니다.** fetch 도구로는 내용이 안 나옵니다. 실경로 확인법:
   ```bash
   curl -sk -X POST "https://<host>/pnuDomain/pnuDomainChk.do" -d "url=<host>"
   # {"siteId":"nonononono"} 이면 죽은 도메인
   ```
2. **User-Agent에 따라 목적지가 바뀌는 호스트가 있습니다.** 백윤주 교수의 `eslab.re.kr`이 그 예로, UA 없음 → 호스팅사 판매 랜딩 / 축약 UA → 목적지 직행 / 실제 Chrome UA → 절반은 트래킹 인터스티셜 경유. **링크 검증은 실제 브라우저 UA를 붙여서 하십시오.** 이 문서의 초기 60개 검증은 UA 없는 curl로 했고, 그래서 이 도메인을 "만료·주차"로 오판했습니다.
3. **프레임셋 사이트를 빈 페이지로 오판합니다.** 우균 교수 `pl.pusan.ac.kr`은 최상위가 269바이트 `<frameset>`이고 본문은 `main.html` 안에 있습니다. `body.innerText` 길이만 보는 자동 검사는 이걸 죽은 링크로 잡습니다. 부산대 구형 연구실 사이트에 더 있을 수 있습니다.
4. **HTTP 200을 반환하는 죽은 사이트가 있습니다.** `abc.pusan.ac.kr`(537B "site move" 스텁 → "중지된 사이트입니다"), `nclab.blogspot.com`(506B 빈 셸 → JS로 404 이동). **상태코드만 보면 살아 있다고 판정됩니다.** 본문·title까지 봐야 합니다.

또한 **학부 페이지 메뉴 ID가 개편으로 죽습니다.** 확인된 것: 정보컴퓨터공학부 `cse/75074`·`cse/14640`·`cse/14660` → 현행 `cse/14215`, 통계학과 `stat/15119`·`stat/51148`·`stat/51418` → 현행 `stat/4796`, 산업공학부 `ie/19404` → 현행 `ie/5865`. 문서에 적힌 URL이 안 열리면 개편을 의심하십시오.

### 기타

- **`docs/faculty-lab-sites.md`의 A/B/C 분류를 임의로 승격시키지 마십시오.** B그룹 URL은 검증이 "사이트 본문의 지도교수명·이메일 대조"뿐이라 공식 근거가 없습니다. 게재하려면 교수 본인 확인이 먼저입니다.
- **`dist/` 재생성 시 `_headers`를 빼먹지 마십시오.** `netlify.toml`은 Netlify용이고 Cloudflare Pages는 읽지 않습니다.
- **백윤주 교수 링크는 포워딩 서비스에 의존합니다.** `eslab.re.kr`이 도메인 포워딩으로 `eslab.pusan.ac.kr/eslab/index.do`에 보내는 구조입니다. 게재된 것은 최종 목적지이므로 포워딩이 끊겨도 안전하지만, 학부 페이지의 링크는 계약 종료 시 깨집니다.

---

## 7. 다음에 해야 할 일

우선순위 순입니다.

### 즉시 확인 (막히면 다른 일을 못 함)

1. **부산대 서버 배포 담당이 누구인지 확인.** 절차가 문서화되어 있지 않고 인수인계 시점에 미확정입니다. 배포 경로를 모르면 어떤 수정도 반영할 수 없습니다.
2. **`fonts/`·`docs/` 보존 및 커밋 여부 결정.** git에 없으므로 클론만 하면 사이트가 깨집니다. 이 인수인계 폴더가 유일한 사본일 수 있습니다.
3. **저장소 접근 권한 이전.** `mslopgk/pnu-ai-college-web`이 개인 계정으로 보입니다(추정). 후임 계정 협업자 추가 또는 조직 이전이 필요합니다.

### 미결 사항 결론내기

4. **미게재 14명 확인 요청 경로 결정 후 발송.** 다른 세션과 중복 연락 방지 조율이 먼저입니다. 손준영·감진규 두 분은 학부 페이지 정정 요청 겸용.
5. **공식 도메인 게재 승인 받기.** 승인 나면 39개 그대로 반영, 반대면 커밋 되돌리기.
6. 확인 완료 시 **연구실 링크 39 → 53명으로 확대** (B그룹 14명 추가).

### 데이터 유지보수

7. **죽은 링크 3명 학부 페이지 정정 요청** (이기준·김정구·탁성우). 학부가 갱신하면 게재 대상이 됩니다.
8. **`docs/faculty-lab-sites.md` 재검증.** 최종 확인이 2026-08-27이고, 초기 60개는 UA 없는 curl로 검증했습니다. 6절의 함정 4가지를 반영해 다시 돌리면 오판이 더 나올 수 있습니다. 특히 A그룹의 구형 호스트(`pl`·`se`·`vb`.pusan.ac.kr)는 프레임셋 여부 확인 필요.
9. **사이트 표기 문구의 확인일 갱신.** 링크 바 하단 "최종 확인 2026-08-27"은 재검증할 때마다 같이 올려야 합니다.

### 코드 정리 (급하지 않음)

10. **`README.md` 보강.** 현재 3줄이고 배포·의존성·주의사항이 없습니다. 이 문서 4절 내용을 옮기는 것이 좋습니다.
11. **캐시 버스팅 자동화 검토.** 지금은 수동이라 빼먹기 쉽습니다.
12. **`app.js` 78KB / `styles.css` 102KB 단일 파일 분리 검토.** 단 6절의 성능 처리들을 되돌리지 않도록 주의.
13. **Netlify 설정 정리 여부 판단.** 실사용이 아니면 `netlify.toml`과 Cloudflare `_headers`가 이원화되어 혼란을 줍니다.

---

## 부록 — 커밋 이력 요약 (32개, 2026-08-23 ~ 08-27)

성격별로 묶으면 이렇습니다. 상세는 `git log`를 보십시오.

| 성격 | 대표 커밋 |
|---|---|
| 초기 구축 | `4bac878` Initial commit |
| 발주처 검토 반영 | `a3bc033`, `f9d4f05`, `3c619e3`, `6fabda6`, `92d104b`, `4526b36` |
| 교수 데이터·사진 | `ad0404b`(54장), `00cdf90`, `ff40bd5`(60/60 완성), `7e830a6`(학과장 지정) |
| 학과 링크 | `aa79b9b`, `0d19dd8`(연구실 38개), `bf2dd44`(39개+출처표기), `227b554` |
| 데이터 정합성 | `091a70f`(일괄치환 회귀 수정), `b3f5376`(학과명 원상복구) |
| 성능 | `130ce61`, `95e3202`, `b93769f`, `62d00c4`, `160c8ea`, `a814015` |
