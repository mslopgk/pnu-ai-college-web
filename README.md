# 부산대학교 AI대학 Page1

`index.html`을 브라우저에서 열거나 정적 서버로 실행합니다.

```powershell
python -m http.server 4173
```

스크롤 스크럽 영상을 적용하려면 MP4 파일을 `assets/hero.mp4`로 넣으면 됩니다. 영상이 없을 때는 제공된 영상 첫 프레임이 표시됩니다. 영상 종료 구간에서 같은 화면 위로 dim과 차례가 나타나며, 그 지점이 홈의 끝입니다.
